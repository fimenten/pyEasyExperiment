# pyEasyExperiment / MLflow example environment variables
# Copy this file to `.env` and adjust values as needed.

# Use a local file-based store by default (no server required)
MLFLOW_TRACKING_URI=file:./mlruns
# For a local MLflow server instead, uncomment this and comment the file-based line above:
# MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# Default MLflow experiment name used by pyEasyExperiment.
PYEASYEXPERIMENT_MLFLOW_EXPERIMENT=demo-experiment

# Optional: basic auth for MLflow tracking server
# MLFLOW_TRACKING_USERNAME=your-username
# MLFLOW_TRACKING_PASSWORD=your-password

# Optional: S3-compatible artifact store (e.g., MinIO)
# MLFLOW_S3_ENDPOINT_URL=http://127.0.0.1:9000
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin
# AWS_DEFAULT_REGION=us-east-1

